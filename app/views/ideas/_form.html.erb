<%= form_for @idea, :html => {:multipart => true} do |f| %>
  <% if @idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

      <ul>
      <% @idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Projekt %><br>
    <%= f.text_field :Projekt %>
  </div>
  <div class="field">
    <%= f.label :Popis %><br>
    <%= f.text_area :Popis %>
  </div>
  <div class="field">
    <%= f.label :Výsledek %><br>
    <%= f.file_field :Výsledek %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
